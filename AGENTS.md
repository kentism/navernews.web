# AGENTS.md

## Overview
이 프로젝트는 **FastAPI + Jinja2 + vanilla JavaScript + SQLite** 기반의 네이버 뉴스 검색/클리핑/학습 보조 웹 애플리케이션이다.

운영 목표는 다음과 같다.

- 최종 클리핑 결과를 안정적으로 학습 데이터로 누적한다.
- 후보 기사 선별이 어떤 학습 근거에 기반하는지 사용자가 확인할 수 있게 한다.
- 새 디플로이, 강력 새로고침, 컨테이너 재시작 이후에도 학습 이력과 백업 상태를 최대한 영속적으로 유지한다.
- 기능을 빠르게 추가하더라도 코드 구조가 흐트러지지 않도록 낮은 결합도와 높은 응집도를 유지한다.

모든 응답은 한국어로 작성한다.

## Core Principles
- 현재 기술 스택을 존중한다. Next.js, React, TypeScript, TailwindCSS, shadcn/ui 등은 명시적 마이그레이션 요청이 없는 한 도입하지 않는다.
- 데이터 로직, API 라우팅, 화면 템플릿, 브라우저 동작을 분리한다.
- 중요한 기능은 구현 전후로 테스트 가능성을 먼저 고려한다.
- 핵심 로직에는 간결한 문서화 주석 또는 Markdown 문서를 남긴다.
- 기능을 추가, 변경, 제거하면 관련 문서를 함께 갱신한다.
- 단순하고 명시적인 상태 관리를 선호한다. 브라우저 상태는 `localStorage` 사용 범위를 명확히 하고, 영속 데이터는 서버 저장소와 백업 흐름을 기준으로 판단한다.
- 사용자 데이터가 손실될 수 있는 작업은 명확한 조건과 복구 경로를 먼저 확인한다.

## Project Structure
현재 프로젝트 구조는 다음 기준을 따른다.

```text
/routers       → FastAPI API 라우터
/services      → 비즈니스 로직, 저장소, 백업, 외부 API 연동
/templates     → Jinja2 HTML 템플릿
/static/js     → 브라우저 동작과 UI 이벤트 처리
/static/css    → 전역 스타일
/utils         → 템플릿 필터와 공통 유틸
/tests         → Python 단위/통합 테스트
/docs          → 기능 설명과 운영 문서
/data          → 로컬 개발용 SQLite 데이터
```

새 폴더를 추가할 때는 기존 구조와 역할이 겹치지 않도록 목적을 명확히 한다.

## Domain Models
핵심 도메인 모델은 다음과 같다.

- `Article`: 검색 API 또는 후보 수집 과정에서 저장된 기사
- `ClippingEvent`: 사용자의 클리핑 또는 최종본 학습 이벤트
- `FinalClippingSnapshot`: 사용자가 완성본으로 확정한 최종 클리핑 문서
- `ClippingCandidate`: 학습 이력을 기반으로 선별된 후보 기사
- `CandidateKeyword`: 후보 수집에 사용하는 검색어
- `LearningSummary`: 누적 학습 상태, 출처/카테고리 통계, 마지막 학습 시점
- `StorageBackup`: GitHub 백업 상태와 복원 가능한 저장소 스냅샷

도메인 모델의 스키마가 바뀌면 관련 저장/복원 로직과 문서를 함께 갱신한다.

## Backend Rules
- `main.py`는 앱 설정, 라우터 등록, 시작 이벤트처럼 앱 조립 역할에 집중한다.
- API 엔드포인트는 `/routers`에 둔다.
- 데이터 저장, 학습 요약, 후보 점수화, 백업/복원 같은 핵심 로직은 `/services`에 둔다.
- 외부 API 호출은 서비스 계층에 격리하고, 테스트에서는 실제 네트워크 호출을 하지 않는다.
- SQLite 스키마 변경은 기존 데이터와 백업 복원 호환성을 고려한다.
- GitHub 백업과 복원 로직은 사용자의 학습 데이터 영속성 관점에서 안전하게 설계한다.
- 컨테이너 재시작 또는 새 디플로이 이후에도 학습 데이터가 복원 가능한지 확인한다.

## Frontend Rules
- Jinja 템플릿은 구조와 초기 HTML 렌더링에 집중한다.
- `/static/js`는 이벤트 바인딩, API 호출, 브라우저 상태 관리에 집중한다.
- `/static/css/style.css`는 현재 전역 스타일 체계를 유지한다.
- React, TailwindCSS, shadcn/ui는 명시적 요청 없이 도입하지 않는다.
- UI 텍스트는 한국어를 기본으로 하며, 인코딩 깨짐이 보이면 해당 파일 전체의 UTF-8 상태를 확인한다.
- 학습/후보/백업 관련 UI는 사용자가 현재 데이터 상태를 육안으로 검증할 수 있게 구성한다.

## Learning And Backup Rules
- “클리핑했다”는 행위만으로 학습했다고 간주하지 않는다.
- 학습 기준은 사용자가 클리핑 탭에서 확정한 **최종본**이다.
- 최종본 학습 직후 서버 DB 저장과 GitHub 백업이 함께 수행되어야 한다.
- 새 디플로이 후 서버 DB가 비어 있고 GitHub 백업에 학습 데이터가 있으면 안전 조건하에 자동 복원을 시도한다.
- 자동 복원 실패 시 사용자가 상태를 확인하고 수동 복원할 수 있는 경로를 제공한다.
- 후보 선별 UI는 점수뿐 아니라 판단 사유와 학습 근거를 함께 보여준다.

## Testing Rules
- 주요 서비스 로직은 `tests/`에 Python 테스트를 둔다.
- 테스트는 단위 테스트와 필요한 범위의 통합 테스트를 우선한다.
- 실제 네트워크 호출은 테스트에서 금지하고 mock 또는 샘플 데이터를 사용한다.
- 저장/복원/백업/학습 요약처럼 데이터 손실 위험이 있는 기능은 테스트 우선 또는 테스트 동반을 원칙으로 한다.
- 프론트엔드 JS를 변경하면 가능한 경우 `node --check`로 문법 검사를 수행한다.

## Documentation Rules
- 기능을 구현하거나 변경하면 관련 Markdown 문서를 갱신한다.
- 운영자가 직접 설정해야 하는 환경변수는 이름, 예시 값, 설정 위치, 영향 범위를 구체적으로 적는다.
- 배포 방식이 GitHub 소스 빌드인지, GHCR Docker image 기반인지 명확히 구분한다.
- 백업/복원 관련 문서는 “현재 서버 DB”, “브라우저 localStorage”, “GitHub 백업”을 구분해서 설명한다.

## Naming And Style
- Python 파일과 함수는 `snake_case`를 사용한다.
- JavaScript 함수는 기존 스타일을 존중하되 명확한 동사형 이름을 사용한다.
- Jinja 템플릿 파일은 현재 파일명 체계를 유지한다.
- API 응답 필드는 일관된 `snake_case`를 사용한다.
- 불필요한 추상화보다 명시적인 함수와 작은 모듈을 선호한다.
- 핵심 함수에는 짧은 주석이나 문서화를 남긴다. 자명한 코드에는 과한 주석을 달지 않는다.

## Git And Deployment
- Conventional Commits를 사용한다.
  - 예: `feat: add learning restore status`
  - 예: `fix: preserve clipping learning after deploy`
- `.env`는 절대 커밋하지 않는다.
- 필요한 경우 `.env.example`을 갱신한다.
- 현재 배포는 Docker/GHCR/Koyeb 흐름일 수 있으므로, 단순 GitHub 푸시가 곧바로 서비스 반영을 의미하지 않을 수 있다.
- 배포 관련 코드를 바꿀 때는 `Dockerfile`, `.github/workflows`, Koyeb 설정 영향까지 함께 고려한다.

## Safety
- 저장소 루트 밖에는 파일을 쓰지 않는다.
- 사용자가 명시적으로 요청하지 않는 한 파괴적 명령을 실행하지 않는다.
- 기존 사용자 변경사항을 임의로 되돌리지 않는다.
- `scratch/` 같은 임시 또는 사용자 실험 파일은 요청이 없는 한 건드리지 않는다.
- 데이터 삭제, 백업 덮어쓰기, 복원 작업은 현재 상태와 백업 상태를 확인한 뒤 수행한다.
